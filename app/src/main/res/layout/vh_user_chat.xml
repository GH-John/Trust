<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="6dp">

    <ImageView
        android:id="@+id/userChatAvatar"
        android:layout_width="@dimen/user_message_avatar_size"
        android:layout_height="@dimen/user_message_avatar_size"
        android:contentDescription="@string/todo"
        tools:src="@android:mipmap/sym_def_app_icon" />

    <TextView
        android:id="@+id/userChatLogin"
        style="@style/DefaultStyleTextRegular"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/userChatAvatar"
        android:layout_alignParentTop="true"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="0dp"
        android:layout_toEndOf="@+id/userChatAvatar"
        android:gravity="center"
        tools:text="@string/hint_login" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/userChatAvatar"
        android:layout_marginStart="42dp"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="42dp"
        android:background="@color/colorGray_50" />
</RelativeLayout>